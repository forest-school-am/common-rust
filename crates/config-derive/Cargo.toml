[package]
name = "common-config-derive"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "#[derive(Config)] for common-config: one struct's fields become its schema and its from_values"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = "2"
