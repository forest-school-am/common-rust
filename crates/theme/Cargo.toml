[package]
name = "common-theme"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The bundled theme palettes (default + standard set) and the browser theme LOADER, carried as committed consts (R117). Apps serve them at /assets/theme-<name>/palette.css and /assets/theme-loader.js. No server-side per-request theme link; the loader parses the single les_theme cookie in the browser."

# No dependencies: this crate is bytes. common-ui-e's build.sh emits the
# palettes and the loader under src/assets/ (committed) and this crate
# include_str!s them. The old theme→templating edge is gone with the retired
# server-side override link.
[dependencies]
