[package]
name = "common-names-derive"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "#[derive(NameColumns)] for common-names: a row struct's #[name(user)]/#[name(group)] fields become its static NameTarget table"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = "2"
