/target

# R11: generated by Les/stand/gen-cargo-patch.sh — names machine-specific
# absolute paths, so never committed. Without it this crate is TESTED against
# the PUBLISHED siblings while its consumers BUILD it against the local ones.
.cargo/config.toml
